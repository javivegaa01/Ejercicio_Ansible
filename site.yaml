- hosts: servidores
  become: true
  tasks:
      # Crear Usuario
    - name: "Creando usuario"
      user:
        name: javier
        password: $6$javier$MdZHUp2y6l5tGGc7qRG8w97kSr8QDc5f0/FpTEQ06qj93n0k6io1m46MGPP/VMVv1ZVwASjk.jNPL1/NcsCPk/
        shell: /bin/bash
        createhome: yes
        home: /home/javier
      #Descargar un fichero
    - name: "Descargando fichero"
      get_url:
        url: "https://wordpress.org/latest.zip"
        dest: /home/usuario/Descargas
      #Descomprimir archivo
    - name: Descomprimiendo archivo.
      unarchive:
        src: wordpress-5.8.1.zip
        dest: /home/usuario/Escritorio
        remote_src: yes.
      # Actualizamos los paquetes apt update;apt upgrade
    #- name: "Actualizamos el sistema"
    #  apt: update_cache=yes upgrade=yes
      # instalar un paquete
    #- name: "Instalar paquete git"
    #  apt:
    #    name: git
      # Copia un fichero a la máquina remoto
    #- name: "Copiar fichero al servidor remoto"
    #  copy:
    #    src: files/foo.conf
    #    dest: /etc/foo.conf
    #    owner: root
    #    group: root
    #    mode: '0644'
    